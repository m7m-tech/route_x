/// Design-system size tokens derived from the Figma canvas (390 × 844).
///
/// Use these with `flutter_screenutil` so every widget scales
/// proportionally across physical screen sizes.
class AppSizes {
  AppSizes._(); // prevent instantiation

  // ── Figma Canvas ──────────────────────────────────────────────────────────

  /// Reference width (dp) of the Figma artboard.
  static const double figmaDesignWidth = 412;

  /// Reference height (dp) of the Figma artboard.
  static const double figmaDesignHeight = 917;
}
